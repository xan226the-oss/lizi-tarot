import Link from "next/link"; import { TarotCardArtwork } from "@/components/library/TarotCardArtwork"; import type { TarotCard } from "@/lib/tarot-cards"; import type { TarotLibraryEntry } from "@/lib/tarot-library"; import type { TarotChatPersona } from "@/lib/tarot-chat-personas"; import styles from "./Chat.module.css";
export function ChatLobby({records}:{records:readonly {card:TarotCard;entry:TarotLibraryEntry;persona:TarotChatPersona}[]}){return <main className={styles.page}><div className={styles.frame}><h1>大阿尔卡纳人格对话</h1><p>选择一张牌面，以象征与隐喻整理当下处境。</p><div className={styles.lobbyGrid}>{records.map(record=><Link className={styles.roleCard} key={record.card.id} href={`/chat/${record.card.id}`}><TarotCardArtwork src={record.entry.imagePath} alt={record.entry.imageAlt} cardName={record.card.name_cn} tone="major" sizes="(max-width:767px) 50vw, 20vw"/><h2>{record.card.name_cn} <small>{record.card.name_en}</small></h2><p>{record.persona.helpAngle}</p><span>进入对话</span></Link>)}</div></div></main>}
